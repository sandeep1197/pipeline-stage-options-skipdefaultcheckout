pipeline {
    agent none
    stages {
        stage('Build') {
			agent any
		       #skipDefaultCheckout()
		}

            steps {                
                echo 'Building master branch'
            }
	}
		
        }
    }
